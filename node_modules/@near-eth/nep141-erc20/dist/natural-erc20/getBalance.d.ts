import { ethers } from 'ethers';
export default function getBalance({ erc20Address, owner, options }: {
    erc20Address: string;
    owner: string;
    options?: {
        provider?: ethers.providers.Provider;
        erc20Abi?: string;
    };
}): Promise<string>;
//# sourceMappingURL=getBalance.d.ts.map